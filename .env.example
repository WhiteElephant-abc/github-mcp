# 必填：GitHub 访问令牌。GitHub code search API 必须认证。
# 只需只读权限：读公开仓库零权限即可；要读私有仓库则加 Contents: Read
GITHUB_TOKEN=

# 可选：服务监听地址与端口
HOST=0.0.0.0
PORT=8000

# 可选：请求重试次数（传输错误指数退避 1/2/4/8...s，默认 3，网络不稳可调大）
# MAX_RETRIES=5

# 可选：API 请求超时秒数（默认 20；raw 大文件下载自动 3 倍且不低于 60s）
# TIMEOUT=20
